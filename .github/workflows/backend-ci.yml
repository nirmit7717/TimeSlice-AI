name: TimeSlice Backend CI Pipeline

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test-and-build:
    runs-on: ubuntu-latest

    services:
      # Optional: set up Redis or other service runtimes if needed

    steps:
    # 1. Check out git repository
    - name: Checkout Source Code
      uses: actions/checkout@v3

    # 2. Configure Python environment
    - name: Set up Python 3.11
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'
        cache: 'pip'

    # 3. Install core packages and test requirements
    - name: Install Project Dependencies
      run: |
        python -m pip install --upgrade pip
        pip install fastapi uvicorn sqlalchemy pyjwt[crypto] requests google-auth google-api-python-client langgraph langchain-core langchain-openai pytest pydantic chromadb

    # 4. Execute test suites for all python packages and apps
    - name: Run Backend Integration & Unit Tests
      env:
        PYTHONPATH: "apps/backend:packages/database:packages/scheduling-system:packages/platform:packages/attention-kernel"
      run: |
        python -m pytest packages/scheduling-system/tests/
        python -m pytest packages/database/tests/
        python -m pytest packages/platform/tests/
        python -m pytest packages/attention-kernel/tests/
        python -m pytest apps/backend/tests/

    # 5. Build Docker image to verify containerization correctness
    - name: Build Production Container Image
      run: |
        docker build -t timeslice-backend:ci -f deployment/docker/production.Dockerfile .
