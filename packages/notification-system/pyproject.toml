[project]
name = "notification-system"
version = "0.1.0"
description = "Multi-channel notification system for TimeSlice AI — APScheduler jobs, desktop alerts, Telegram"
requires-python = ">=3.11"
dependencies = [
    "apscheduler>=3.10.0",
    "requests>=2.31.0",
    "plyer>=2.1.0",
]
