[project]
name = "adaptive-intelligence"
version = "0.1.0"
description = "Contextual Bandit adaptive scheduling intelligence for TimeSlice AI"
requires-python = ">=3.11"
dependencies = [
    "numpy>=1.24.0",
    "pydantic>=2.0.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
